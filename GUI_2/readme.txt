Compile and run GUITest to test the graphical interface.
Command handlers are auxiliary classes which separate GUI design from commands handling. You should be able to use it into the game as an interface to game mechanics.
Packages are omitted, but remember to import classes correctly after they're integrated into the main project.